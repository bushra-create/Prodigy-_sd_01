This program converts temperatures between Celsius, Fahrenheit, and Kelvin scales.  For example
program should prompt the user to input a temperature and orignal unit of measurement. It should convert the value into other units.

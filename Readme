This program converts temperatures between Celsius, Fahrenheit, and Kelvin scales. The
Build a Temperature should then convert the temperature Conversion Program the converted values to the user. For
program should prompt the user to input a temperature value and the original unit of measurement. It
to the other two units and display
example, if the user enters a temperature of 25 degrees Celsius, the program should convert it to Fahrenheit and Kelvin, and present the converted values as outputs.
